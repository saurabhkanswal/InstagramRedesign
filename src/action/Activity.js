import database from '@react-native-firebase/database';
import storage from '@react-native-firebase/storage';

import {SET_ACTIVITY} from './action.types';

export const setActivity = () => async (dispatch, state) => {};
